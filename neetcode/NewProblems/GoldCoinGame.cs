namespace neetcode.NewProblems;
public static class GoldCoinGame
{
    /// <summary>
    /// BRAIN DUMP. CLOSE YOUR EYES.
    /// n was the amount of elements ... e.g. array elements ... p was the position of the gold coin e.g. 1st position (aka not zero indexed), 
    /// m which was the minimal amount of positions you can select, and x which is the location that a player must take the gold coin to win 
    /// (also not zero indexed). Given the input ... either return Steve or Harvey or draw. The problem is that two players are playing a game
    /// and the goal is to get the gold coin to position x on their turn, but they have to select m adjacent coins and include the gold element. 
    /// ALso when you select m elements you can only reverse those positions ... that's how you move. Each turn the player must make a move.
    /// </summary>
    /// <param name="n"></param>
    /// <param name="p"></param>
    /// <param name="m"></param>
    /// <param name="x"></param>
    /// <returns></returns>
    //public static string Solve(int n, int p, int m, int x)
    //{

    //}
}
